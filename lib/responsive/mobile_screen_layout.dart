import 'package:flutter/material.dart';


class MobileSceenLayout extends StatelessWidget {
  const MobileSceenLayout({Key? key}) : super(key: key);
 
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Version Mobile'),
      ),
    );
  }
}