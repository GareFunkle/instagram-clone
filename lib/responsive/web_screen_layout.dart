import 'package:flutter/material.dart';


class WebSceenLayout extends StatelessWidget {
  const WebSceenLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Version Web'),
      ),
    );
  }
}